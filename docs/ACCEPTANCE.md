# Requirements coverage

This maps the supplied “Face ID + Blockchain Verification” document to implementation and distinguishes runnable source from live evidence that must still be collected.

| Requirement | Implementation/check | Live evidence status |
| --- | --- | --- |
| Runtime input photo | `py/orchestrator.py` positional image, consent gate, byte snapshot | Consenting photo needed |
| Local face detection and embedding | `py/embed.py`, Facenet512/OpenCV, 512-vector kept in memory | Model loads; real consenting photo needed |
| No/multiple/small/blurred face rejection | One-face/size/blur gates; Python checks and real blank-image smoke | Local checks available |
| Genuine external reverse-image search | `py/reverse_search.py` calls Google `web_detection()` at runtime | Google credentials and indexed image needed |
| No hardcoded post results | No runtime fixture mode; only API response or explicitly human-operated live fallback | Real returned social URL needed |
| Full/partial matching page extraction | Both page image lists, PAGE_MATCH fallback, exact URL retention | Live API response needed |
| Candidate URL normalization and social filtering | Valid public HTTP(S), deduplication keys, seven specified platform domains | Human must inspect public page |
| Candidate face comparison and ranking | Input embedding reused; ordered social FULL/PARTIAL candidates | Actual candidate bytes needed |
| Real model distance and threshold | Cosine distance/threshold/model/status recorded; no confidence percentage | Real candidate result needed |
| Blocked media reported unavailable | Bounded public download; unavailable distinct from not_verified | Locally checked logic |
| Failed face match skipped | Cannot pass candidate gate after negative comparison | Regression checked |
| Human confirmation | Candidate review + final publication prompt; no yes bypass | Operator must confirm live |
| Canonical record | Sorted compact UTF-8 JSON; rejects nonfinite floats | Generated by confirmed run |
| Commitments | SHA-256 original photo bytes and exact URL; SDK Merkle root for JSON | Local hashing checked |
| 0G Storage upload | Maintained official SDK; validates upload response and root | Funded wallet and live upload needed |
| Proof-checked download | SDK proof flag plus recomputed file root; before registry submission and during read-back | Real storage root needed |
| Solidity registry | Append-only roots/commitments/submitter/timestamp; event and counter | Local EVM tests pass; Galileo deployment needed |
| Galileo chain 16602 | Pinned configuration, network guards, explorer URLs | Public chain ID reached; wallet/deployment needed |
| Deployment/write/read commands | `deploy.ts`, `writeRecord.ts`, `readRecord.ts`; receipt ID parsing | Live contract and transaction needed |
| Transaction hash output | Printed immediately on broadcast; journal and mined result saved | Live transaction needed |
| Independent read-back | `py/verify_record.py`: chain, storage proof, original hashes, exact record bytes | Live record needed |
| SQLite index | Complete schema, parameterized insert, chain identity, verification status | Local integration check passes |
| Visible failures and retained evidence | Per-run status, errors, provider evidence, canonical record, storage result, transaction journal | Failure ordering regression checked |
| Manual fallback | Interactive Lens/Yandex attestation, separate provenance and provider name | Only available after API search yields no confirmed candidate |
| README/install/version pins | README, `.env.example`, npm workspaces/lock, requirements/lock, Python version file | Included |
| Known limitations and scope | All seven supplied limitations plus operational boundaries in README | Included |
| Secrets ignored | `.env`, credentials, keys, `secrets/`, photos, output, DB, videos ignored | Do not place secrets under arbitrary unignored filenames |
| Unedited end-to-end recording | `docs/DEMO.md`, recording destination | **Pending actual live run and human recording** |

The two final live outcomes—finding a real matching public social page and recording a real testnet commitment—cannot be checked off using mocked tests or local EVM transactions. Update this table with the actual run's evidence after completing the recording.
